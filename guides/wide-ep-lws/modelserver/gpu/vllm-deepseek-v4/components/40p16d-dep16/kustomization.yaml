apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
# SGLang-parity topology: 5 prefill workers (DEP8) + 1 decode worker (DEP16)
# -> 5x8 + 1x16 = 56 GPUs / 14 nodes total.
# Prefill worker: TP=1, DP=8,  EP=8  (DEP8,  2 nodes).
# Decode  worker: TP=1, DP=16, EP=16 (DEP16, 4 nodes).
patches:
  - target:
      kind: LeaderWorkerSet
      name: ".*-prefill"
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 5
  - target:
      kind: LeaderWorkerSet
      name: ".*-decode"
    patch: |-
      - op: replace
        path: /spec/leaderWorkerTemplate/size
        value: 4
